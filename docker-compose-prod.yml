version: "3.9"

services:
  app:
    container_name: ${PROD_APP_CONTAINER_NAME:-react-app-prod}
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "${PROD_APP_PORT:-80}:80"
